name: SAST

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '23 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sast:
    uses: sparkrock-eng/shared-workflows/.github/workflows/security-sast.yml@main
    with:
      timeout_minutes: 45
